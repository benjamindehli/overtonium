build/
build-*/
cmake-build-*/
_deps/

# Where the release workflow stages the installers and zips, and where
# gh run download puts them when they are fetched back to attach by hand.
archives/
.claude/
CLAUDE.md
temp/

# macOS
.DS_Store
*.xcodeproj/
*.xcworkspace/
DerivedData/

# Windows
*.vcxproj*
*.sln
*.user

# Prettier is the only thing here that needs node, and it is pinned in
# package-lock.json, which is committed so CI installs the same version.
node_modules/

# Editors
.vscode/
.idea/
compile_commands.json
